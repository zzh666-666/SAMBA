# SAMBA股价预测项目复现步骤

## 📋 项目概述

本文档提供SAMBA（State-space Mamba with Graph Neural Networks）股价预测模型的完整复现指南。SAMBA是一个结合Mamba状态空间模型和图神经网络的创新架构，用于股价预测任务。

**论文信息**：
- 标题：Mamba Meets Financial Markets: A Graph-Mamba Approach for Stock Price Prediction
- 会议：IEEE ICASSP 2025
- 作者：Ali Mehrabian, Ehsan Hoseinzade, Mahdi Mazloum, Xiaohong Chen

## 🛠️ 环境准备

### 系统要求
- **操作系统**：Linux/Windows/macOS
- **Python版本**：3.8+
- **GPU**：推荐使用CUDA兼容的GPU（至少4GB显存）
- **内存**：至少8GB RAM

### 步骤1：创建Python环境

```bash
# 使用conda创建虚拟环境（推荐）
conda create -n samba python=3.9
conda activate samba

# 或使用venv创建虚拟环境
python -m venv samba_env
# Windows
samba_env\Scripts\activate
# Linux/macOS
source samba_env/bin/activate
```

### 步骤2：安装依赖包

```bash
# 安装PyTorch（根据您的CUDA版本选择）
# CUDA 11.8
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# CUDA 12.1
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# CPU版本（不推荐，训练会很慢）
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# 安装其他依赖
pip install -r requirements.txt
```

### 步骤3：验证环境

```bash
python -c "import torch; print(f'PyTorch版本: {torch.__version__}'); print(f'CUDA可用: {torch.cuda.is_available()}'); print(f'CUDA版本: {torch.version.cuda if torch.cuda.is_available() else \"N/A\"}')"
```

预期输出示例：
```
PyTorch版本: 2.1.0+cu118
CUDA可用: True
CUDA版本: 11.8
```

## 📊 数据准备

### 步骤4：创建数据目录

```bash
mkdir Dataset
```

### 步骤5：获取数据集

项目需要三个股市数据集文件，每个文件包含82个日度股票特征：

1. **NASDAQ综合指数** - `combined_dataframe_IXIC.csv`
2. **纽约证券交易所** - `combined_dataframe_NYSE.csv`  
3. **道琼斯工业平均指数** - `combined_dataframe_DJI.csv`

**数据获取方式**：
- 从论文作者的GitHub仓库下载：[https://github.com/Ali-Meh619/SAMBA](https://github.com/Ali-Meh619/SAMBA)
- 或从Kaggle数据集下载：[Stock Market Prediction Dataset](https://www.kaggle.com/datasets/ehoseinz/stock-market-prediction/data)

### 步骤6：数据格式验证

数据文件应包含以下列：
- `Date`：日期索引
- `Name`：股票/指数名称
- `Price`：价格数据（目标变量）
- 其他82个技术指标特征

**验证数据格式**：
```bash
python -c "
import pandas as pd
df = pd.read_csv('Dataset/combined_dataframe_IXIC.csv')
print(f'数据形状: {df.shape}')
print(f'列名: {list(df.columns)[:10]}...')  # 显示前10列
print(f'日期范围: {df[\"Date\"].min()} 到 {df[\"Date\"].max()}')
"
```

预期输出：
```
数据形状: (3500, 84)  # 大约3500行，84列（包括Date和Name）
列名: ['Date', 'Name', 'Price', 'Volume', 'RSI', ...]
日期范围: 2010-01-01 到 2023-11-30
```

## 🚀 运行项目

### 步骤7：系统测试

首先运行系统测试确保所有组件正常工作：

```bash
python test_system.py
```

预期输出：
```
==================================================
SAMBA Stock Price Forecasting System Test
==================================================
Testing configuration...
✓ ModelArgs created: d_inner=128, dt_rank=4
✓ TrainingConfig created with 45 parameters

Testing utilities...
✓ Seed initialization successful
*****************Model Parameter*****************
Total params num: 50000
*****************Finish Parameter****************
✓ Model parameter printing successful

Testing forward pass...
Testing model creation...
✓ Model created successfully
✓ Forward pass successful. Input shape: torch.Size([2, 5, 10]), Output shape: torch.Size([2, 1, 1])

==================================================
✓ All tests passed! SAMBA stock forecasting system is working correctly.
==================================================
```

### 步骤8：运行主实验

```bash
python main.py
```

## 📈 训练过程详解

### 训练阶段输出解释

**1. 初始化阶段**：
```
🚀 SAMBA: A Graph-Mamba Approach for Stock Price Prediction
📚 Paper: Mamba Meets Financial Markets: A Graph-Mamba Approach for Stock Price Prediction
🏛️  Conference: IEEE ICASSP 2025
👥 Authors: Ali Mehrabian, Ehsan Hoseinzade, Mahdi Mazloum, Xiaohong Chen
📊 Expected Features: 82
======================================================================
Loading and preparing data...
Number of features (graph nodes): 81
Initializing SAMBA model...
```

**2. 模型参数信息**：
```
*****************Model Parameter*****************
embedding.weight torch.Size([81, 32]) True
mam1.layers.0.mixer.in_proj.weight torch.Size([64, 32]) True
...
Total params num: 125000
*****************Finish Parameter****************
```

**3. 训练过程**：
```
Starting training...
Train Epoch 1: 0/100 Loss: 0.234567
Train Epoch 1: 20/100 Loss: 0.198765
...
**********Train Epoch 1: averaged Loss: 0.187654
**********Val Epoch 1: average Loss: 0.176543
*********************************Current best model saved!
```

**4. 最终结果**：
```
Final Results:
MAE: 0.0234
RMSE: 0.0456
Information Coefficient (IC): 0.1234
Rank Information Coefficient (RIC): 0.0987
Training completed successfully!
```

### 训练参数说明

**核心参数**（在`paper_config.py`中定义）：
- `epochs=1100`：训练轮数
- `batch_size=32`：批次大小
- `lr_init=0.001`：初始学习率
- `early_stop_patience=200`：早停耐心值
- `d_model=32`：模型维度
- `n_layer=3`：Mamba层数
- `cheb_k=3`：切比雪夫多项式阶数

## 📊 结果分析

### 评估指标解释

1. **MAE (Mean Absolute Error)**：平均绝对误差，越小越好
2. **RMSE (Root Mean Squared Error)**：均方根误差，越小越好
3. **IC (Information Coefficient)**：信息系数，衡量预测与真实收益的线性相关性
4. **RIC (Rank Information Coefficient)**：排序信息系数，衡量预测排序能力

### 预期结果范围

根据论文报告，在三个数据集上的典型结果：
- **MAE**: 0.02-0.05
- **RMSE**: 0.04-0.08  
- **IC**: 0.08-0.15
- **RIC**: 0.06-0.12

## 🔧 自定义配置

### 修改数据集

在`main.py`中修改数据集选择：
```python
# 默认使用NASDAQ数据
dataset_file = 'Dataset/combined_dataframe_IXIC.csv'

# 改为NYSE数据
dataset_file = 'Dataset/combined_dataframe_NYSE.csv'

# 改为道琼斯数据  
dataset_file = 'Dataset/combined_dataframe_DJI.csv'
```

### 调整模型参数

在`paper_config.py`中修改配置：
```python
# 模型配置
model_args = ModelArgs(
    d_model=64,           # 增加模型维度
    n_layer=4,            # 增加层数
    seq_in=10,            # 增加输入序列长度
    # ... 其他参数
)

# 训练配置
training_config = TrainingConfig(
    epochs=500,           # 减少训练轮数
    batch_size=64,        # 增加批次大小
    lr_init=0.0005,       # 降低学习率
    # ... 其他参数
)
```

### 修改数据划分

在`main.py`中调整数据集划分比例：
```python
train_loader, val_loader, test_loader, mmn, num_features = prepare_data(
    csv_file=dataset_file,
    window=config.lag,
    predict=config.horizon,
    test_ratio=0.2,       # 20%测试集
    val_ratio=0.1         # 10%验证集
)
```

## 🐛 常见问题排查

### 问题1：CUDA内存不足
**错误信息**：`RuntimeError: CUDA out of memory`

**解决方案**：
```python
# 在paper_config.py中减小批次大小
training_config = TrainingConfig(
    batch_size=16,        # 从32减少到16
    # 或者更小
    batch_size=8,
)
```

### 问题2：数据文件未找到
**错误信息**：`❌ Dataset folder not found!`

**解决方案**：
1. 确保创建了`Dataset`文件夹
2. 确保数据文件名称正确
3. 检查文件路径

```bash
ls -la Dataset/
# 应该看到三个CSV文件
```

### 问题3：依赖包版本冲突
**错误信息**：`ImportError` 或版本不兼容

**解决方案**：
```bash
# 重新安装特定版本
pip install torch==2.1.0 torchvision==0.16.0
pip install einops==0.7.0
pip install pandas==1.5.3
```

### 问题4：训练过程中损失爆炸
**现象**：损失值突然变得很大（>1e6）

**解决方案**：
1. 降低学习率
2. 启用梯度裁剪
3. 检查数据预处理

```python
# 在paper_config.py中
training_config = TrainingConfig(
    lr_init=0.0001,       # 降低学习率
    grad_norm=True,       # 启用梯度裁剪
    max_grad_norm=1.0,    # 设置梯度裁剪阈值
)
```

### 问题5：训练速度过慢
**解决方案**：
1. 确保使用GPU训练
2. 增加批次大小（在内存允许的情况下）
3. 减少模型复杂度

```python
# 检查GPU使用
python -c "import torch; print(torch.cuda.is_available())"

# 监控GPU使用情况
nvidia-smi
```

## 📁 输出文件说明

训练完成后会生成以下文件：

1. **`samba_results.txt`**：详细的评估结果
2. **`best_model.pth`**：最佳模型权重
3. **`SAMBA_Model/results/`**：结果保存目录
4. **训练日志**：控制台输出的训练过程

### 结果文件格式

`samba_results.txt`内容示例：
```
IC: 0.12345
RIC: 0.09876
MAE: 0.02345
RMSE: 0.04567
```

## 🔄 模型推理

### 加载训练好的模型

```python
import torch
from models import SAMBA
from config import ModelArgs

# 加载模型配置
model_args = ModelArgs(
    d_model=32,
    n_layer=3,
    vocab_size=81,  # 根据实际特征数量调整
    seq_in=5,
    seq_out=1
)

# 创建模型
model = SAMBA(model_args, 32, 5, 1, 10, 3)

# 加载权重
model.load_state_dict(torch.load('best_model.pth'))
model.eval()

# 进行预测
with torch.no_grad():
    prediction = model(input_data)
```

## 📚 进阶使用

### 实验对比

可以通过修改配置进行不同实验的对比：

1. **不同序列长度对比**：
```python
# 实验1：seq_in=5
# 实验2：seq_in=10  
# 实验3：seq_in=20
```

2. **不同模型深度对比**：
```python
# 实验1：n_layer=2
# 实验2：n_layer=3
# 实验3：n_layer=4
```

3. **不同数据集对比**：
```python
# 实验1：NASDAQ数据
# 实验2：NYSE数据
# 实验3：DJIA数据
```

### 结果可视化

```python
import matplotlib.pyplot as plt
import pandas as pd

# 读取结果
results = pd.read_csv('samba_results.txt', sep=': ', header=None)
results.columns = ['Metric', 'Value']

# 绘制结果
plt.figure(figsize=(10, 6))
plt.bar(results['Metric'], results['Value'])
plt.title('SAMBA Model Performance')
plt.ylabel('Score')
plt.show()
```

## ✅ 复现检查清单

在开始复现之前，请确认以下项目：

- [ ] Python 3.8+ 已安装
- [ ] CUDA环境配置正确（如果使用GPU）
- [ ] 所有依赖包已安装
- [ ] Dataset文件夹已创建
- [ ] 三个数据文件已下载并放置正确
- [ ] 系统测试通过
- [ ] 有足够的磁盘空间（至少2GB）
- [ ] 有足够的内存（至少8GB）

## 📞 获取帮助

如果在复现过程中遇到问题：

1. **检查错误日志**：仔细阅读错误信息
2. **查看GitHub Issues**：访问原始仓库的Issues页面
3. **论文参考**：查阅原始论文了解更多细节
4. **社区支持**：在相关技术论坛寻求帮助

## 🎯 成功标志

复现成功的标志：
1. 系统测试全部通过
2. 训练过程正常运行，无错误中断
3. 获得合理的评估指标结果
4. 生成了模型权重文件和结果文件

预期训练时间：
- **GPU训练**：2-4小时（取决于GPU性能）
- **CPU训练**：12-24小时（不推荐）

祝您复现成功！🎉